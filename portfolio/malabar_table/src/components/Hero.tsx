import { motion, useReducedMotion } from 'framer-motion'
import { ArrowDownRight } from 'lucide-react'
import { whatsappUrl } from '../config/contact'
export function Hero() { const reduced = useReducedMotion(); return <section className="hero-section" id="home"><motion.div className="hero-image" initial={reduced ? false : { scale: 1.06 }} animate={reduced ? undefined : { scale: 1 }} transition={{ duration: 1.4, ease: 'easeOut' }} /><div className="hero-shade" /><div className="hero-content"><motion.p className="eyebrow light" initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .18 }}>A contemporary taste of Kerala</motion.p><motion.h1 initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .28, duration: .7 }}>Malabar <em>Table</em></motion.h1><motion.p className="hero-lede" initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: .52 }}>Traditional flavours of Malabar, thoughtfully prepared and served with warmth.</motion.p><motion.div className="hero-actions" initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .68 }}><a href="#menu" className="button button-cream">Explore Menu <ArrowDownRight size={17} /></a><a href={whatsappUrl} target="_blank" rel="noreferrer" className="text-button">Reserve a Table</a></motion.div></div><a className="scroll-cue" href="#story">Scroll to discover <span>↓</span></a></section> }
